`model_id`
:   (Required, string) The ID of the deployed text embedding model, or the ID of a model deployment. The model must produce dense vectors with the same dimensions as the target `dense_vector` field.

`model_text`
:   (Required, string) The text string from which the model generates the dense vector representation.
